import { CartContent } from '@/components/cart-content';

export const metadata = {
  title: 'Cart — DigiStore',
  description: 'Review your cart and checkout.',
};

export default function CartPage() {
  return <CartContent />;
}